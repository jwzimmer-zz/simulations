#i did some monte carlo simulations in college but have since forgotten a lot and want to make sure i can still
#understand the concepts : )


def montecarlopi(datapointsinsimulation, radius=1):
    """take the radius of a circle, which will also be the side of (1/4 of) the square,
    and the amount of data wanted to be in the simulation
    note: will look at 1/4 of the square and circle because it's easier"""
    #pi will come from the ratio of the square's area and circle's area:
    #circle = (pi*r^2)/4
    #square = (r^2)/4
    #since r != 0 --> pi = circle/ (pi*r^2/4) = circle/ square
    return None
